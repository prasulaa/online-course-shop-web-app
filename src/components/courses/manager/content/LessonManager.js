import { Button, IconButton, Paper, SvgIcon } from "@mui/material";

export default function LessonManager(props) {

    return (
        <Paper
            variant='outlined'
            square
        >
            SectionManager
        </Paper>
    );
}