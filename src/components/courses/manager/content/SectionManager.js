import { Paper } from "@mui/material";

export default function SectionManager(props) {

    return (
        <Paper
            variant='outlined'
            square
        >
            SectionManager
        </Paper>
    );
}